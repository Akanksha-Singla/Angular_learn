import { CurrecyPipePipe } from './currecy-pipe.pipe';

describe('CurrecyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrecyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
